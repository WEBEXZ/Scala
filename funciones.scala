object Persona {
  def saludar(nombre: String): Unit = {
    println("Hola " + nombre)
  }
  def mostrarSuma(x: Int): Unit = {
    println(x)
  }
  def sumarEnteros(x: Int, y: Int): Int = {
    x + y
  }
}
